docker:image:build-and-publish:stage:builder:
    extends: .docker:image:build-and-publish:stage
    variables:
        BUILD_CONTEXT: docker-images/builder
        DOCKER_REGISTRY_USERNAME: gitlab-ci-token
        DOCKER_REGISTRY_PASSWORD: $CI_BUILD_TOKEN
        DOCKER_REGISTRY_URL: $CI_REGISTRY
        IMAGE_NAME: $CI_REGISTRY_IMAGE/builder
        IMAGE_TAG: alpine
    only:
        changes:
            - docker-images/builder/**/*
